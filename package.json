{
	"name": "@covcom/root",
	"version": "3.1.0",
	"author": "xero (https://x-e.ro)",
	"description": "COVCOM: Covert communications for private group conversations. Invite, talk, close the client, and the chat vanishes. Every message is encrypted with XChaCha20 and signed with Ed25519. A BLAKE3 fingerprint on each key allows peers to verify one another. SPQR's manual and epoch ratchets add forward secrecy, while post-quantum ML-KEM-768 encapsulation keeps recorded communications unreadable and secure against future cryptanalysis.",
	"license": "MIT",
	"private": true,
	"type": "module",
	"workspaces": [
		"server",
		"web",
		"cli",
		"lib"
	],
	"scripts": {
		"build:cli": "bun scripts/build.ts cli",
		"build:cli:all": "bun scripts/build.ts cli --targets all",
		"build:docker": "sh ./docker/run build",
		"build:server": "bun scripts/build.ts server",
		"build:server:all": "bun scripts/build.ts server --targets all",
		"build:web": "bun scripts/build.ts web",
		"bundle:cli": "bun run --cwd cli bundle",
		"bundle:server": "bun run --cwd server bundle",
		"dev": "bun scripts/dev.ts",
		"dev:cli": "bun run --cwd cli dev",
		"dev:server": "bun run --cwd server dev",
		"dev:web": "bun run --cwd web dev",
		"fix": "bunx eslint . --fix",
		"lint": "bunx eslint .",
		"run:docker": "sh ./docker/run up",
		"start:server": "bun run --cwd server start",
		"test": "bun run --parallel --no-exit-on-error --filter '@covcom/*' test",
		"test:all": "bun run test && bun run test:cross && bun run test:e2e",
		"test:cli": "bun run --cwd cli test",
		"test:cross": "bun run build:cli && bun test ./web/test/cross/web-cli.cross.ts",
		"test:e2e": "playwright test",
		"test:lib": "bun run --cwd lib test",
		"test:server": "bun run --cwd server test",
		"test:server:bin": "bun run --cwd server build && COVCOM_SERVER_BIN=dist/covcom-server bun run --cwd server test",
		"test:web": "bun run --cwd web test",
		"typecheck": "bun scripts/build.ts all --codegen && bunx tsc --noEmit -p tsconfig.json && bun run --parallel --no-exit-on-error --workspaces --if-present typecheck",
		"versionbump": "bun scripts/tombstone.ts --versionbump",
		"_shorthand": "# bake=build+stage · check=codegen+lint+typecheck+build+unit+e2e",
		"bake": "bun scripts/build.ts all --kind npm --targets all",
		"check": "bun scripts/build.ts all --codegen && bun run lint && bun typecheck && bun bake && bun run test:all"
	},
	"packageManager": "bun@1.3.14",
	"devDependencies": {
		"eslint": "^10.4.1",
		"playwright": "^1.60.0",
		"@playwright/test": "^1.60.0",
		"@eslint/js": "^10.0.1",
		"@types/node": "^25.9.2",
		"bun-types": "^1.3.14",
		"jiti": "^2.7.0",
		"typescript-eslint": "^8.61.0"
	},
	"keywords": [
		"XChaCha20",
		"ML-KEM-768",
		"Ed25519",
		"BLAKE3",
		"post-quantum",
		"pqc",
		"kyber",
		"ML-KEM",
		"SPQR",
		"E2EE",
		"ephemeral",
		"N-party",
		"cryptography",
		"encryption",
		"crypto",
		"chat",
		"groupchat",
		"messenger",
		"forward-secrecy",
		"double-ratchet",
		"end-to-end-encryption",
		"CLI",
		"TUI",
		"SPA",
		"bun",
		"covcom",
		"chat",
		"communications",
		"covert",
		"anonymous",
		"incognito"
	]
}
