{
	"name": "@covcom/web",
	"private": true,
	"type": "module",
	"scripts": {
		"dev":       "vite",
		"build":     "bun build.ts --kind spa",
		"preview":   "vite preview",
		"test":      "bun build.ts && bun test",
		"typecheck": "bunx --no-install tsc --noEmit -p tsconfig.json && bunx --no-install tsc --noEmit -p tsconfig.tools.json && bunx --no-install tsc --noEmit -p test/e2e/tsconfig.json && bunx --no-install tsc --noEmit -p test/cross/tsconfig.json"
	},
	"dependencies": {
		"@covcom/lib": "workspace:*"
	},
	"devDependencies": {
		"@happy-dom/global-registrator": "^20.9.0",
		"@types/bun": "latest",
		"vite": "^8.0.14",
		"vite-plugin-singlefile": "^2.3.3"
	}
}
